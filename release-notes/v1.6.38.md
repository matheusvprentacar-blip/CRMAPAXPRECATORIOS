# v1.6.38

# Release 2026-03-18 (Build Fix)

- Corrigida incompatibilidade de build com `output: export` na rota `/api/market/latest`, removendo dependÃªncia de cookies/auth e configurando resposta estÃ¡tica com revalidaÃ§Ã£o.
- Removida rota dinÃ¢mica `/api/precatorios/[id]/simulate` (incompatÃ­vel com export estÃ¡tico) e criada rota estÃ¡vel `/api/precatorios/simulate`.
- Atualizado o painel de projeÃ§Ã£o comparativo para usar o novo endpoint `/api/precatorios/simulate`, enviando `precatorioId` no payload.
- Validado localmente com `npm run build` completo, incluindo geraÃ§Ã£o e exportaÃ§Ã£o estÃ¡tica sem erros.
