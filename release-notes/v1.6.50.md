# v1.6.50

fix(atendimento): corrige valor dividido por 1000 na importacao em lote. O parseBRL agora trata o ponto como separador de milhar quando nao ha virgula decimal (ex.: 366.910 -> 366910 em vez de 366,91), cobrindo os numeros formatados em padrao BR que o Excel entrega via raw:false.
