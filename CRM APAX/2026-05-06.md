# 2026-05-06

## Retorno de cálculo para triagem e fila global

- Contexto: ajuste solicitado para o operador de cálculo visualizar todos os créditos disponíveis para cálculo, abrir detalhes e devolver créditos para triagem quando faltarem dados.
- Áreas impactadas: fila `/calculo`, calculadora `/calcular`, Kanban, RLS de `precatorios` e documentação do fluxo de cálculo.
- Arquivos alterados: `app/(dashboard)/calculo/page.tsx`, `app/(dashboard)/kanban/page.tsx`, `components/calculador-precatorios.tsx`, `supabase/migrations/20260506110000_248_operador_calculo_fila_global_triagem.sql`.
- Decisão: `calculo_andamento` passa a ser aplicado apenas após conclusão da Etapa 1 da calculadora; abrir a tela não muda mais o status.
- Decisão: retorno para `triagem_interesse` exige motivo obrigatório e registra observação/atividade quando possível.
- Pendências: aplicar a migration no Supabase antes de validar o acesso real com usuário `operador_calculo`.

## Bloqueio do React Grab em produção

- Contexto: usuário normal estava vendo o React Grab após deploy na Vercel.
- Áreas impactadas: layout global Next.js e deploy web.
- Arquivos alterados: `app/layout.tsx`, `CRM APAX/Sistema/Arquitetura Técnica.md`.
- Decisão: scripts `react-grab` e `@react-grab/codex` só carregam em desenvolvimento local com `NEXT_PUBLIC_ENABLE_REACT_GRAB=true`.
- Validação: `npm run build` passou e a busca em `.next` não encontrou `react-grab` nos artefatos de produção.
- Deploy: produção publicada na Vercel em `2026-05-06`, com alias ativo em `https://precatorios.grupoapax.com`.
- Validação pós-deploy: `/login` no domínio público respondeu `200` e não contém `react-grab` no HTML.
- Pendências: nenhuma para esta correção.
