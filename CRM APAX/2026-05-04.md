---
title: 2026-05-04
tags:
  - diario
  - codex
  - projeto
---

# 2026-05-04

## Atualizações

### Correção da persistência do cálculo na tabela `precatorios`
- Contexto: ao finalizar o cálculo, o sistema falhava informando ausência da coluna `irpf_total` em `precatorios`.
- Mudanças:
  - validação do schema real do Supabase para comparar as colunas existentes com o payload enviado por `components/calculador-precatorios.tsx`
  - identificação das colunas legadas que o frontend ainda tentava gravar: `irpf_total`, `pss_total`, `menor_proposta`, `maior_proposta`, `taxa_juros_moratorios` e `qtd_salarios_minimos`
  - ajuste da persistência para usar as colunas canônicas do banco: `irpf_valor`, `pss_valor`, `proposta_menor_valor`, `proposta_maior_valor`, `proposta_menor_percentual`, `proposta_maior_percentual`, além dos percentuais e flags fiscais do cálculo
  - manutenção de `taxa_juros_moratorios` e `qtd_salarios_minimos` apenas dentro de `dados_calculo`, já que não existem colunas dedicadas no schema atual
  - atualização das notas [[Fluxo de Cálculo]] e [[Tabelas Principais]] para refletir o mapeamento correto da persistência
- Arquivos/áreas: `components/calculador-precatorios.tsx`, `CRM APAX/Processos/Fluxo de Cálculo.md`, `CRM APAX/Banco de Dados/Tabelas Principais.md`
- Decisão tomada: corrigir o mapeamento do salvamento para o schema vigente em vez de recriar colunas legadas que não são consumidas pelo restante do sistema.
- Testes executados:
  - `npx eslint components/calculador-precatorios.tsx`
  - validação programática do payload contra as colunas reais da tabela `precatorios` no Supabase
- Pendências: validar em ambiente de uso o fluxo completo de `finalizar cálculo` para confirmar atualização visual imediata em detalhes, kanban e histórico.

### Deploy em produção no Vercel
- Contexto: publicação da versão atual do workspace em produção após a correção da persistência do cálculo.
- Mudanças:
  - build local validado com `npm run build`
  - deploy executado com `vercel deploy --prod --yes`
  - deployment publicado com status `READY` em `https://crmapaxprecatorios-qpx5cb5up-grupo-apax.vercel.app`
  - alias de produção confirmado em `https://precatorios.grupoapax.com`
  - resposta HTTP validada no domínio com redirecionamento `307` de `/` para `/dashboard` e `200 OK` na página final
- Arquivos/áreas: operação de infraestrutura/deploy; atualização documental em `CRM APAX/Sistema/Arquitetura Técnica.md`
- Decisão tomada: manter o fluxo de deploy direto via Vercel CLI para publicar rapidamente o estado atual do workspace.
- Pendências: acompanhar o warning recorrente de build em `lib/server/nova-pipeline-ocr.ts` sobre `pdf-parse` para evitar regressão futura na rota `app/api/atendimento/ocr-lote/route.ts`.
