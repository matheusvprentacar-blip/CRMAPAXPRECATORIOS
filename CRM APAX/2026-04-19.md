---
title: 2026-04-19
tags:
  - diario
  - codex
  - projeto
---

# 2026-04-19

## Atualizações

### Regra do Codex integrada ao Obsidian
- Contexto: definido o vault `CRM APAX` como memória operacional do projeto para leitura e registro das mudanças.
- Mudanças:
  - atualização do `AGENTS.md` para obrigar consulta ao índice e às notas relevantes do Obsidian antes de mudanças importantes
  - criação da nota [[Memória Operacional do Codex]] com as regras de leitura e escrita do vault
  - inclusão da nova nota no [[🏠 Índice]] para facilitar navegação e adoção da rotina
- Arquivos/áreas: `AGENTS.md`, `CRM APAX/🏠 Índice.md`, `CRM APAX/Sistema/Memória Operacional do Codex.md`
- Pendências: validar nas próximas tarefas se será útil criar notas temáticas extras por módulo conforme o volume de mudanças crescer

### Protocolo Claude Codex criado
- Contexto: estruturado um canal de handoff entre Claude e Codex usando o vault do Obsidian para dividir frontend, backend, correções e outras frentes.
- Mudanças:
  - atualização de `AGENTS.md` e `CLAUDE.md` com a regra de tarefa compartilhada
  - criação de [[Painel Claude Codex]] com o fluxo de colaboração
  - criação do template `CRM APAX/Agentes/Templates/TEMPLATE - Tarefa Compartilhada.md`
  - criação do comando `npm run agent:handoff` para abrir uma nota padronizada de handoff
  - configuração do plugin de templates do Obsidian para `Agentes/Templates`
- Arquivos/áreas: `AGENTS.md`, `CLAUDE.md`, `package.json`, `scripts/create-agent-handoff.mjs`, `CRM APAX/Agentes/`
- Pendências: validar o uso nas próximas trocas reais entre frontend e backend e ajustar os campos da nota se o fluxo pedir mais granularidade

### Fluxo de cálculo sincronizado com Obsidian
- Contexto: necessário transformar o Obsidian na fonte detalhada do passo a passo do cálculo e refletir esse conteúdo dentro da calculadora.
- Mudanças:
  - reescrita da nota [[Fluxo de Cálculo]] com um nó mestre destacado, etapas detalhadas, fórmulas, diferenças entre UI e engine e regras de sincronização
  - criação do canvas [[Mapa Visual do Cálculo.canvas|Mapa Visual do Cálculo]] com nós separados apontando para os headings da mesma nota
  - atualização da nota [[Calculadora de Precatórios]] para refletir as 8 etapas reais e os drawers laterais
  - criação da rota `app/api/knowledge/calculo-flow/route.ts` para ler a nota do Obsidian em tempo real
  - criação de `lib/knowledge/calculo-flow.ts` para parsear a nota e estruturar os dados
  - criação do drawer `GuiaCalculoViewer` dentro da calculadora, carregando o fluxo diretamente do vault
- Arquivos/áreas: `CRM APAX/Processos/Fluxo de Cálculo.md`, `CRM APAX/Módulos/Calculadora de Precatórios.md`, `components/calculador-precatorios.tsx`, `components/calculo/guia-calculo-viewer.tsx`, `app/api/knowledge/calculo-flow/route.ts`, `lib/knowledge/calculo-flow.ts`
- Pendências: se houver necessidade futura, transformar partes do conteúdo do Obsidian em regras executáveis da engine, além do guia informativo atual

### Atendimento virou a porta de entrada dos créditos comerciais
- Contexto: o fluxo de inclusão deixou de acontecer em `/admin/precatorios` e passou a nascer diretamente no setor de atendimento, com distribuição imediata para operadores.
- Mudanças:
  - remoção dos modais de inclusão/importação da página `app/(dashboard)/admin/precatorios/page.tsx`
  - criação do fluxo de cadastro manual e importação em lote dentro de `app/(dashboard)/atendimento/page.tsx`
  - distribuição obrigatória já no cadastro/importação com preenchimento de `responsavel`, `dono_usuario_id` e trilha de distribuição admin
  - inclusão da tag operacional `operator_tag` em `usuarios` com os valores `operador`, `freelancer` e `externo`
  - visibilidade ajustada no atendimento para esconder valores de agentes e operadores, mantendo valores somente para admin
  - criação da nota [[Setor de Atendimento]] e atualização das notas de papéis, módulo admin, ciclo de vida e banco de dados
- Arquivos/áreas: `app/(dashboard)/atendimento/page.tsx`, `components/atendimento/modal-novo-credito-atendimento.tsx`, `components/atendimento/modal-importacao-lote.tsx`, `app/(dashboard)/admin/usuarios/`, `app/(dashboard)/admin/precatorios/page.tsx`, `hooks/use-usuarios-cache.ts`, `lib/users/operator-tag.ts`, `lib/atendimento/distribuicao-creditos.ts`, `supabase/migrations/20260419110000_244_operator_tag_usuarios.sql`, `CRM APAX/`
- Decisão tomada: manter o atendimento como fila fechada até haver interesse do credor, com segmentação operacional por tag e balanceamento automático para lotes grandes.
- Pendências: validar em ambiente real se o balanceamento por `valor_principal` atende bem os lotes maiores e decidir depois se vale adicionar distribuição por capacidade máxima por operador

### Hotfix de compatibilidade para `operator_tag`
- Contexto: em ambientes onde a migration de `operator_tag` ainda não rodou, o login quebrava no carregamento de perfil com erro `42703` (`column usuarios.operator_tag does not exist`).
- Mudanças:
  - fallback no `AuthProvider`: tenta query com `operator_tag` e, em erro de coluna inexistente, refaz sem a coluna
  - fallback no `useUsuariosCache` com a mesma estratégia para evitar erro em telas de usuários/atendimento
  - helper central `isMissingOperatorTagColumnError` para identificar o erro de schema de forma segura
- Arquivos/áreas: `lib/auth/auth-context.tsx`, `hooks/use-usuarios-cache.ts`, `lib/users/operator-tag.ts`, `CRM APAX/Sistema/Arquitetura Técnica.md`
- Decisão tomada: priorizar tolerância a rollout parcial de banco, sem bloquear login enquanto a migration não chega em todos os ambientes.
- Pendências: executar a migration `20260419110000_244_operator_tag_usuarios.sql` no ambiente afetado para voltar ao comportamento completo com tags operacionais.

### Remoção da coluna "Recebidos do Admin" no Kanban
- Contexto: simplificação do quadro Kanban para eliminar a etapa intermediária `Recebidos do Admin`.
- Mudanças:
  - remoção da coluna `recebidos_admin` em `app/(dashboard)/kanban/columns.ts`
  - ajuste no agrupamento para que créditos distribuídos pelo admin e ainda em `entrada` passem a aparecer em `triagem_interesse` para operador comercial
  - remoção da opção `Recebidos do Admin` dos filtros avançados de status
  - atualização da documentação do módulo Kanban
- Arquivos/áreas: `app/(dashboard)/kanban/columns.ts`, `app/(dashboard)/kanban/page.tsx`, `components/precatorios/advanced-filters.tsx`, `CRM APAX/Módulos/Kanban.md`
- Decisão tomada: reduzir o número de colunas e manter visibilidade operacional jogando o fluxo direto para triagem.
- Pendências: validar com o time comercial se algum card de `entrada` sem distribuição precisa de tratamento visual adicional no Kanban.

### Remoção da coluna visual "Jurídico" no Kanban
- Contexto: necessidade de simplificar o quadro removendo a coluna própria do Jurídico, sem interromper o fluxo jurídico.
- Mudanças:
  - remoção da coluna `juridico` em `app/(dashboard)/kanban/columns.ts`
  - mapeamento do status `juridico` para aparecer dentro da coluna `analise_processual_inicial` via `statusIds`
  - ajuste da regra de envio para cálculo para tratar `juridico` e `analise_processual_inicial` com a mesma validação de permissão
  - atualização da documentação do módulo Kanban
- Arquivos/áreas: `app/(dashboard)/kanban/columns.ts`, `app/(dashboard)/kanban/page.tsx`, `CRM APAX/Módulos/Kanban.md`
- Decisão tomada: manter a lógica jurídica ativa no backend/status, reduzindo apenas a complexidade visual do quadro.
- Pendências: validar com o time jurídico se desejam distinção visual futura por badge dentro da coluna de Pré-análise.

### Encerrados unificados com pausa obrigando data de retorno
- Contexto: pausados, reprovados e não elegíveis precisavam ficar na mesma coluna visual, com tratamento específico para pausa.
- Mudanças:
  - unificação de status `pausado_credor`, `pausado_documentos`, `sem_interesse`, `reprovado` e `nao_elegivel` na coluna `encerrados`
  - modal de pausa com modo dedicado, exigindo motivo e `data_recontato` obrigatória
  - fluxo de `sem_interesse` mantido com motivo obrigatório e recontato opcional
  - ajuste do KPI de ativos para não contar status encerrados (`pos_fechamento`, pausas, sem interesse, reprovado e não elegível)
  - correção das aberturas do modal de sem interesse para sempre inicializar `mode` e `destinoStatus`
- Arquivos/áreas: `app/(dashboard)/kanban/columns.ts`, `app/(dashboard)/kanban/page.tsx`, `components/kanban/modal-sem-interesse.tsx`, `CRM APAX/Módulos/Kanban.md`
- Decisão tomada: centralizar todos os casos não ativos em `Encerrados`, mas com validação rígida para pausas para garantir follow-up comercial.
- Pendências: validar com operação se desejam um lembrete automático adicional (job diário) além da notificação já criada no momento do agendamento.

### Triagem com 5 fluxos e contato obrigatório via Atendimento
- Contexto: a triagem de interesse precisava aceitar apenas fluxos operacionais definidos e bloquear avanço sem evidência de contato vindo do setor de atendimento.
- Mudanças:
  - triagem da tela de detalhes do precatório ajustada para os fluxos: `normal`, `pausado`, `encerrado`, `nao_elegivel` e `credito_vendido`
  - fluxo `pausado` reaproveita modal com data de retorno obrigatória e motivo
  - fluxo `encerrado` reaproveita modal de sem interesse
  - para `origem = atendimento`, o fluxo `normal` só avança após contato `interessado` válido no Atendimento
  - `atendimento_tentativas` recebeu os campos `contato_nome`, `contato_telefone` e `interesse_receber_proposta`
  - modal de nova tentativa no Atendimento passou a exigir esses campos quando resultado for `interessado`
  - botão rápido de `Interessado` no Atendimento agora abre o modal de contato (sem bypass)
- Arquivos/áreas: `app/(dashboard)/precatorios/detalhes/page.tsx`, `app/(dashboard)/atendimento/page.tsx`, `components/atendimento/modal-nova-tentativa.tsx`, `components/atendimento/historico-tentativas.tsx`, `supabase/migrations/20260419123000_245_atendimento_interesse_contato_obrigatorio.sql`, `CRM APAX/`
- Decisão tomada: padronizar a origem da decisão comercial em um registro estruturado do Atendimento para garantir rastreabilidade e consistência antes de avançar no pipeline.
- Pendências: aplicar migration `245` no banco e validar em homologação o fluxo completo `Atendimento -> Detalhes -> docs_credor`.

### Migração para acesso web-only (remoção de runtime Tauri)
- Contexto: decisão técnica de desativar o uso do Tauri por risco operacional, padronizando o acesso exclusivamente via Chrome.
- Mudanças:
  - remoção de integrações `@tauri-apps/*` no runtime do app (layout, telemetria, notificações e consulta DataJud)
  - remoção dos componentes de updater desktop (`GlobalUpdateNotifier`) e adaptação do `UpdateChecker` para fluxo web
  - ajuste da telemetria para origem operacional `web` no frontend e remoção dos filtros/labels de Tauri/Híbrido no painel admin
  - remoção das dependências Tauri do `package.json` e atualização do `package-lock.json`
  - ajuste do `next.config.js` para operação web (sem comentário/regra de export específico para Tauri)
- Arquivos/áreas: `app/layout.tsx`, `app/(dashboard)/layout.tsx`, `app/(dashboard)/configuracoes/page.tsx`, `app/(dashboard)/admin/telemetria/page.tsx`, `components/settings/update-checker.tsx`, `components/telemetry/telemetry-provider.tsx`, `components/notifications/useNotifications.tsx`, `components/precatorios/datajud-panel.tsx`, `components/settings/global-update-notifier.tsx` (removido), `components/notifications/desktop-notifications-listener.tsx` (removido), `package.json`, `package-lock.json`, `next.config.js`, `tsconfig.json`, `CRM APAX/Sistema/*`
- Decisão tomada: manter apenas canal web para reduzir superfície de falha e eliminar dependência do bridge desktop.
- Pendências: revisar e ajustar posteriormente o pipeline de release/docs ainda orientado a Tauri (`.github/workflows/release.yml`, scripts de release e documentação legada).

### Release final do desktop com bloqueio e redirecionamento para web
- Contexto: solicitação para publicar uma última atualização Tauri que inutiliza o app desktop e orienta o acesso via web.
- Mudanças:
  - criação do bloqueio full-screen em runtime Tauri com URL oficial `https://precatorios.grupoapax.com`
  - inclusão de script de build dedicado para Tauri (`build:tauri`) com `TAURI_BUILD=1`
  - ajuste de `next.config.js` para gerar `out/` somente quando `TAURI_BUILD=1`
  - ajuste do `src-tauri/tauri.conf.json` para usar `beforeBuildCommand: npm run build:tauri`
  - release publicada com commit, tag e push via script oficial (`npm run release:update`)
- Arquivos/áreas: `components/tauri/desktop-migration-blocker.tsx`, `app/layout.tsx`, `package.json`, `package-lock.json`, `next.config.js`, `src-tauri/tauri.conf.json`, `release-notes/v1.6.45.md`
- Decisão tomada: manter o desktop apenas como canal de descontinuação orientado à migração, com operação oficial no Chrome.
- Pendências: acompanhar execução da Action de release para confirmar artefatos e `latest.json` no GitHub Release da tag `v1.6.45`.

### Correção do build da Action de release (v1.6.45)
- Contexto: a Action da tag `v1.6.45` falhou no `beforeBuildCommand` do Tauri porque `build:tauri` usava `next build` com export estático, incompatível com rotas API dinâmicas (`/api/knowledge/calculo-flow`).
- Mudanças:
  - substituição do `build:tauri` para script dedicado de descontinuação (`node ./scripts/build-tauri-shutdown.mjs`)
  - remoção do `output` condicional no `next.config.js` para evitar regressão com App Router + `app/api`
  - geração do `out/index.html` e `out/404.html` estáticos com aviso de migração para `https://precatorios.grupoapax.com`
  - validação local completa: `npm run build` e `npm run build:tauri` com sucesso
- Arquivos/áreas: `package.json`, `next.config.js`, `scripts/build-tauri-shutdown.mjs`, `CRM APAX/Sistema/Arquitetura Técnica.md`
- Decisão tomada: para o desktop final, usar artefato HTML estático de desligamento em vez de tentar exportar toda a aplicação Next.
- Pendências: publicar nova tag de release para reexecutar a Action com o fluxo corrigido.

### Deploy em produção no Vercel (web-only)
- Contexto: publicação da versão atualizada em produção após release `v1.6.46`.
- Mudanças:
  - deploy executado via CLI com `vercel deploy --prod --yes`
  - build de produção concluído com sucesso no projeto `crmapaxprecatorios`
  - alias/domínio de produção confirmado em `https://precatorios.grupoapax.com`
- Arquivos/áreas: operação de infraestrutura/deploy (sem mudança de código adicional nesta etapa).
- Decisão tomada: manter o deploy web em produção como canal oficial único de acesso.
- Pendências: acompanhar logs/telemetria pós-deploy para detectar regressões de runtime.
