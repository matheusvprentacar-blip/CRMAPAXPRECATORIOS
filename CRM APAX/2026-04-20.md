---
title: 2026-04-20
tags:
  - diario
  - codex
  - projeto
---

# 2026-04-20

## Atualizações

### Atendimento: operador vê apenas créditos distribuídos para si (inclui arquivados)
- Contexto: ajuste solicitado para garantir que, no módulo de atendimento, o operador visualize somente os créditos distribuídos para ele também na aba de arquivados.
- Mudanças:
  - ajuste da regra de escopo em `app/(dashboard)/atendimento/page.tsx` para priorizar filtro operacional sempre que o usuário tiver role `operador_comercial` ou `operador` (exceto `admin`)
  - manutenção do filtro por distribuição (`responsavel`/`dono_usuario_id`) para toda a consulta da tela, refletindo em `Fila Ativa` e `Arquivados`
  - atualização da documentação do módulo para registrar comportamento em combinações de roles
- Arquivos/áreas: `app/(dashboard)/atendimento/page.tsx`, `CRM APAX/Módulos/Setor de Atendimento.md`
- Decisão tomada: em casos de múltiplas roles, o escopo operacional deve prevalecer para evitar exposição de carteira de outros operadores no atendimento.
- Testes executados: `npm run build` (sucesso)
- Pendências: validar com um usuário de role combinada (`agente_atendimento` + `operador`) em ambiente real para confirmar a experiência esperada.

### Deploy em produção (Vercel)
- Contexto: publicação da correção de escopo do atendimento após commit e push da branch.
- Mudanças:
  - deploy executado via `vercel deploy --prod --yes`
  - nova URL de produção gerada: `https://crmapaxprecatorios-93moqjxnj-grupo-apax.vercel.app`
  - alias de domínio aplicado: `https://precatorios.grupoapax.com`
- Arquivos/áreas: operação de infraestrutura/deploy (sem alteração adicional de código).
- Decisão tomada: manter o fluxo de publicação via Vercel CLI diretamente da branch atual.
- Pendências: monitorar comportamento da aba `Arquivados` para perfis com roles combinadas.
